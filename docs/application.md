## Application

### `MyPOSApplication`
- Extends `Application`.
- Initializes Firebase on app start with basic logging.

Usage:
- Declare `android:name=".MyPOSApplication"` in `AndroidManifest.xml` `application` tag.